version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "bg-app-td"
        LoadBalancerInfo:
          ContainerName: "bg-app"
          ContainerPort: 80
